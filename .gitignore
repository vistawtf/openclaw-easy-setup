# Secrets — never commit these
.env
.env.*
**/*.key
**/*.pem
**/*.secret
**/api_key
**/credentials*

# OpenClaw state (lives in ~/.openclaw/, not here)
openclaw.json
auth.json
sessions.json
secrets.json
auth-profiles.json
**/secrets*
*.jsonl

# OS
.DS_Store
Thumbs.db

# Temp/debug
*.tmp
*.bak
screenshots/
debug/
